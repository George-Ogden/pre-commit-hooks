name: Check README for Version

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  check-readme:
    uses: George-Ogden/actions/.github/workflows/readme-check.yaml@v4.2.0
    with:
      prefix: "rev: v"
